const swaggerJsdoc = require('swagger-jsdoc');

const options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'FinTech API',
      version: '1.0.0',
      description: 'Simple FinTech API with balance, deposit, withdraw and transfer operations',
    },
    servers: [
      {
        url: 'http://localhost:3000',
      },
    ],
  },
  apis: ['./index.js'], // <-- Points to where Swagger comments are written
};

const swaggerSpec = swaggerJsdoc(options);
module.exports = swaggerSpec;
